var matrix=document.getElementById("matrix");var context=matrix.getContext("2d");matrix.height=window.innerHeight;matrix.width=window.innerWidth;var drops=[];var fontSize=16;var columns=matrix.width/fontSize;for(var i=0;i<columns;i++){drops[i]=1}function drawMatrix(){context.fillStyle="rgba(0, 0, 0, 0.1)";context.fillRect(0,0,matrix.width,matrix.height);context.fillStyle="green";context.font=fontSize+"px";for(var i=0;i<columns;i++){context.fillText(Math.floor(Math.random()*2),i*fontSize,drops[i]*fontSize);if(drops[i]*fontSize>(matrix.height*2/3)&&Math.random()>0.85){drops[i]=0}drops[i]++}}setInterval(drawMatrix,40);$(window).resize(function(){matrix.height=window.innerHeight;matrix.width=window.innerWidth;columns=matrix.width/fontSize;for(var i=0;i<columns;i++){drops[i]=1}});
